{
	"hooks": {
		"PostToolUse": [
			{
				"matcher": "Write|Edit",
				"hooks": [
					{
						"type": "command",
						"command": "(bun run format || true) && (uv run ruff format . || true)"
					}
				]
			}
		]
	}
}
